import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  txtCode: {
    fontSize: 14,
    color: '#fff',
  },
  linear: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
